pipeline {
    agent {
        label 'test-agent'
    }

    stages {

        stage('Pull Image') {
            steps {
                sh '''
                docker pull karthik19112001/website:latest
                '''
            }
        }

        stage('Run Container') {
            steps {
                sh '''
                docker rm -f test-container || true
                docker run -d --name test-container -p 8081:80 karthik19112001/website:latest
                '''
            }
        }

        stage('Trigger Prod Job') {
            steps {
                build job: 'Job3-Prod'
            }
        }
    }
}
